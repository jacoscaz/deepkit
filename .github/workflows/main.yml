name: CI

on:
  pull_request:
  push:

jobs:
  unit-tests:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Install
        run: npm install && npm run postinstall

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test:coverage
